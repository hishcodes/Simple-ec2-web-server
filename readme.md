A simple Terraform project to setup a VPC, subnet, ec2 instance which hosts a static webpage to display the internal IP of the instance and is accessed using elastic ip.

![Webpage Image](webpage.png)